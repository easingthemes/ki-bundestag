---
agent: 'agent'
description: 'Load project context at session start — git status, recent commits, and progress files if available.'
---

Load project context at the start of a new session. Check git status, current branch, and recent commits. If progress files exist (`Progress.md`, `*-progress.md`), include active work status. Don't start working — just present context and ask what to work on.

Reference: .claude/skills/context/SKILL.md for full prompt details.
