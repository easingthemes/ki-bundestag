# dx Plugin Configuration
# Generated by /dx-init — edit values to match your project.

dx:
  version: "2.86.2"                  # Plugin version — used for config migrations

project:
  name: "ki-bundestag"
  prefix: "ki"
  type: "frontend"                     # aem-fullstack | aem-frontend | frontend | java | ...
  role: "fullstack"                    # frontend | backend | fullstack | config

scm:
  provider: github                     # ado | github
  org: "easingthemes"
  project: "ki-bundestag"
  repo-id: ""                          # Auto-discovered, or set manually
  base-branch: "main"                  # e.g., develop, development, main

# Work item tracker (defaults to scm.provider if not set)
# tracker:
#   provider: github

build:
  command: "npm run build"             # Full build
  deploy: ""                           # Quick deploy, skip tests
  test: "npm test"                     # Unit tests
  lint: "npm run lint"                 # Lint
  typecheck: "npm run typecheck"       # Typecheck
  # serve: "npm run dev:api & npm run dev:web"

# toolchain: — detected by /dx-adapt, describes the project's build stack
#   node: "18.0.0"
#   build-tool: vite
#   css-compiler: tailwindcss
#   js-transpiler: tsc
#   template-engine: jsx
#   module-system: esm

# Cross-repo awareness — sibling repos in your project ecosystem
# repos:
#   - name: My-Backend-Repo
#     path: ../My-Backend-Repo
#     role: backend

preferences:
  auto-commit: true
  auto-pr: false
