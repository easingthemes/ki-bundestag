Show project status:
1. Run `git status` and `git log --oneline -5`
2. Check if `Progress.md` exists — if so, show current step status
3. Check if dev servers are running (`lsof -i :3001` and `lsof -i :5173`)
4. Summarize: branch, uncommitted changes, progress, servers